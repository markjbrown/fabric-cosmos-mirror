{
    // Intentionally empty.
    // This repository supports multiple languages/frameworks.
    // Open the appropriate `*.code-workspace` file at the repo root
    // (e.g. `Go.code-workspace`) to get the right interpreter,
    // envFile, and debug configuration for that language.
}
